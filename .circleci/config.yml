##### TEST #####

version: 2
jobs:
  one:
    docker:
      - image: circleci/ruby:2.4.1
    steps:
      - checkout
      - run: echo "A first hello"
      - run: sleep 25
  test:
    docker:
      - image: circleci/ruby:2.4.1
    steps:
      - checkout
      - run: fastlane scan
  deploy:
    docker:
      - image: circleci/ruby:2.4.1
    steps:
      - checkout
      - deploy:
          #name: Deploy
          command: fastlane gym

workflows:
  version: 2
  test_release:
    jobs:
      - one
      - test
      - deploy:
          requires:
            test
